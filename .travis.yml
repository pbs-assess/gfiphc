# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

# From sizeSpectra (from gfplot) .travis.yml, though taken out covr:

warnings_are_errors: false

r_packages:
  - devtools
